name: Sync Frases Store on Stage

on:
  workflow_dispatch:
    inputs:
      apply:
        description: Apply seed and rewrite JSON mirror
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

jobs:
  sync-frases-store-stage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code (stage)
        uses: actions/checkout@v5
        with:
          ref: stage

      - name: Setup SSH key
        env:
          VPS_SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s' "$VPS_SSH_KEY" > ~/.ssh/id_vps
          chmod 600 ~/.ssh/id_vps

      - name: Add VPS to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Run sync_frases_store.py on stage
        env:
          APPLY: ${{ github.event.inputs.apply }}
        run: |
          ssh -i ~/.ssh/id_vps -o IdentitiesOnly=yes ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "export APPLY='${APPLY}'; bash -s" <<'EOF'
            set -euo pipefail
            STAGE_DIR="/var/www/fortcordis-stage"

            cd "$STAGE_DIR/backend"
            source venv/bin/activate
            if [ -f .env ]; then
              set -a
              source .env
              set +a
            fi

            if [ "${APPLY}" = "true" ]; then
              python3 sync_frases_store.py --apply
            else
              python3 sync_frases_store.py
            fi
          EOF
