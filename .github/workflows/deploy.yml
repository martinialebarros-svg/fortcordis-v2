name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
        chmod 600 ~/.ssh/deploy_key
        ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

    - name: Test SSH connection
      run: |
        ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no -o BatchMode=yes -v ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "echo 'SSH OK'"

    - name: Deploy to VPS
      run: |
        ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'EOF'
          cd /var/www/fortcordis-v2
          
          # Backup antes do deploy
          echo "📦 Criando backup..."
          tar -czf backup_$(date +%Y%m%d_%H%M%S).tar.gz backend frontend 2>/dev/null || true
          
          # Puxar últimas alterações
          echo "⬇️  Atualizando código..."
          git fetch origin
          git reset --hard origin/main
          
          # Deploy Backend
          echo "🐍 Deploy Backend..."
          cd backend
          source venv/bin/activate 2>/dev/null || python3 -m venv venv && source venv/bin/activate
          pip install -r requirements.txt
          if [ -f .env ]; then
            set -a
            source .env
            set +a
          fi
          python3 setup_database.py || echo "⚠️  setup_database.py falhou, revisar logs"
          sudo systemctl restart fortcordis-backend 2>/dev/null || echo "⚠️  Reinicie manualmente"
          cd ..
          
          # Deploy Frontend
          echo "⚛️  Deploy Frontend..."
          cd frontend
          npm install
          API_BACKEND_URL=http://127.0.0.1:8000 npm run build
          sudo systemctl restart fortcordis-frontend 2>/dev/null || echo "⚠️  Reinicie manualmente"
          cd ..
          
          echo "✅ Deploy concluído!"
        EOF
